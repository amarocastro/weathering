<Page
    x:Class="weathering.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:weathering.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Background="{ThemeResource SystemAltMediumColor}">

    <Grid Margin="12">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<StackPanel Margin="0,0,0,46">
			<TextBlock Grid.Row="0" Text="Options" FontSize="24"/>
		</StackPanel>
		<StackPanel Grid.Row="1">
			<TextBlock Text="Providers" Style="{ThemeResource BaseTextBlockStyle}"/>
			<RadioButton x:Name="here_weather" 
						 Tag="here_weather"
						 Click="provider_selected_Click"
						 Content="Here Weather"/>
			<RadioButton x:Name="weather_io" 
						 Tag="weather_io"
						 Click="provider_selected_Click"
						 Content="Weather.io"/>
		</StackPanel>
		
    </Grid>
</Page>
